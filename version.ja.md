# 変更履歴

新しい版が上に並びます。各項目は、マージされたプルリクエストの一覧ではなく、利用者にとって何が
変わったかを書きます。

リリースの説明文は、タグと一致する見出しの節から取り出します。項目は変更のたびに `## Unreleased`
の節へ書き足し、リリースを準備するときに見出しをバージョン番号へ書き換えます。`Unreleased` に
一致するタグはないため、書き足したままタグを付けるとワークフローが失敗します。中身の空のリリース
が公開されることはありません。手順は [README.ja.md](README.ja.md) の「リリース手順」を参照して
ください。

この文書は [version.md](version.md) の日本語訳です。英語版と同じコミットで更新します。

## Unreleased

- まだ一度もリリースしていません。最初のリリースは `v0.1.0` です。
- ウィンドウを開くアプリから、タスクトレイに常駐するアプリになりました。起動しても画面には何も
  出ません。時計の横にアイコンが出て、そのアイコンのメニューに「Capture」「Settings」
  「Quit Driveshot」が並びます。設定ウィンドウを閉じても終了せず、非表示になるだけです。終了は
  メニューから行います。
- 取り込みのキーは **Ctrl+Shift+D**（Macでは **Cmd+Shift+D**）です。どの画面にいても使えます。
  事前にウィンドウを開く必要はありません。キーの組み合わせはまだ変更できません。
- 他のアプリがそのキーをすでに使っている場合は、その旨をお知らせします。押しても何も起きない
  状態のままにはしません。起動時に設定ウィンドウを開き、何が原因かを表示します。
- **キーを押すと画面を取り込めるようになりました。** すべての画面が暗くなります。どれか1つの上で
  範囲をドラッグすると、その部分が保存されます。Escキー、右クリック、ドラッグせずにクリックした
  場合は、何も保存せずに元に戻ります。
- 保存先は、**ピクチャフォルダーの中の Driveshot フォルダー**です。ファイル名は撮った時刻から
  付きます（例：`driveshot-20260920-143052.png`）。現時点では、ここに残ったままになります。
- 取り込みや保存に失敗した場合は、黙って終わらせません。設定ウィンドウが開いて、原因を表示します。
  Macでは、画面収録の許可を断った場合もここに表示されます。この許可は、初回利用時にmacOSが確認します。
- 設定ウィンドウに、最後に保存した場所を表示します。ファイルを探しに行かなくても、成功したかどうかが
  分かります。
- キーを押したときに、画面が一瞬白くなってから暗くなる現象を修正しました。
- 画面を暗くする範囲が、画面の端まで届くようになりました。これまでは左右それぞれ10ピクセルほど
  手前で止まっており、保存される画像も選択した範囲から少しずれていました。
- キーを押すと、すぐに画面が暗くなるようになりました。これまでは約1秒かかっていました。押した時点で
  画面ごとのウィンドウを作っていたためです。このウィンドウは、押す前にあらかじめ用意するようにしました。
- **アップロード、共有、削除はまだ実装していません。** 取り込んだ画像は、お使いのパソコンの中だけに
  あります。実装の順序は [README.ja.md](README.ja.md) の「今後の予定」に記載しています。
