# 設計方針

[English version](architecture.md)

## リポジトリの構成

3つの層に分かれます。上の2つの境界が、他のすべての判断の基準になります。

```
src/                      設定ウィンドウ。HTML・CSS・TypeScript。受け取った答えを表示する。
  ↓  invoke()
src-tauri/                アプリ本体。常駐、ホットキー、画面取り込み、クラウド通信、ファイル操作。
  ↓  通常の関数呼び出し
crates/driveshot-core/    判断を行う層。ウィンドウ・ネットワーク・OSのAPIに依存しない。
```

呼び出しは必ず下向きです。core側はTauriを知らず、ウィンドウ側はアップロードの仕組みを知りません。

### なぜcoreを分けるか

`crates/driveshot-core` の内容は、どのOSでもビルドでき、テストも実行できます。このプロジェクトの
作業で多く使うLinuxでも同じです。これは手間を減らすためではなく、Driveshotの中で最も危険な処理を
検証可能にするためです。「保存期間を過ぎた」という判断は、誤れば利用者の画像を消します。そして
この判断は、時刻を受け取って結果を返すだけの関数として書けます。実際にそう書き、境界値ごとに
テストを用意しています。

追加時の判断基準は次のとおりです。画面・ファイル・ネットワークなしで書けるなら、core側に置きます。
`src-tauri` に残すのは、それらがどうしても必要な部分だけです。

### 現在coreにあるもの

| 型 | 何を判断するか |
|---|---|
| `Provider` | どのクラウドドライブへ保存したか。記録用の識別子も持つ |
| `Retention` | 保存期間と、削除の対象になる時刻 |
| `ShotRecord` | 1件のアップロード記録。ファイル、保存先、時刻、共有URL、保存期間 |
| `ShotIndex` | 未削除のアップロード全件と、そのうち削除対象となるもの |

次の2点は意図的な設計です。理由なく変更しないでください。

- **保存期間0日は表現できません。** `Retention::Days` は `NonZeroU32` を持つため、入力欄が空の場合
  や設定ファイルに `0` が入っていた場合でも、「いまアップロードしたものを即削除する」という意味に
  はなりません。呼び出し側それぞれで確認するのではなく、型が受け付けません。
- **日付として表現できない期限は、削除しない扱いにします。** 非常に大きな日数を加算すると、
  暦の表現範囲を超えます。この場合は「期限なし」として扱い、「期限切れ」とは扱いません。後者では、
  利用者が保持を指定したファイルを削除してしまうためです。

### バージョン番号の置き場所

`src-tauri/Cargo.toml` の `[package]` セクションだけです。`src-tauri/tauri.conf.json` には意図的に
バージョンを書いていないため、Tauriのバンドラーはここを読みます。`.github/workflows/release.yml`
が、タグの値でこの1行を書き換えます。2か所に書けば、2か所で書き忘れが起きます。

## まだ実装していない機能

Driveshotの目的である4つの機能は、いずれも未実装です。

1. 画面の一部を**取り込む**。
2. OAuth認証を通じてクラウドドライブへ**アップロードする**。
3. ファイルを「リンクを知っている人は閲覧可」に設定し、URLを取得して**共有する**。
4. 保存期間を過ぎたら**削除する**。

それぞれIssueを用意しています。画面の取り込みは [#5](https://github.com/kaorinstar/driveshot/issues/5)、
最初のクラウドドライブ一連の流れは [#6](https://github.com/kaorinstar/driveshot/issues/6)、
記録の保存と削除は [#7](https://github.com/kaorinstar/driveshot/issues/7)、
これらの前提となるトレイ常駐とホットキーは [#4](https://github.com/kaorinstar/driveshot/issues/4) です。

現在のウィンドウは、3つの保存先を一覧表示し、いま撮った画像がいつ削除されるかを表示します。
どちらの答えもcoreが計算し、`src-tauri/src/main.rs` の2つのTauriコマンド経由で渡しています。
これは意図した骨組みです。難しい処理を書く前に、ウィンドウからcore、テスト、CI、インストーラー
までの経路を実際に動く形で確認できます。

## 決定事項：1点は決定、1点は未決定

どちらも、決めなくても開発は進められます。ただし決め方によって作るものが変わるため、記録して
おきます。このうち1点は決まりました。

### 最初に対応する保存先：Googleドライブのみ（[#2](https://github.com/kaorinstar/driveshot/issues/2)）

**決定しました。** Googleドライブを最初に、単独で実装します。OneDriveとDropboxはその後、#8 で
対応します。

Googleドライブ、OneDrive、Dropboxは、認証方法・アップロード方法・共有URLの発行方法がそれぞれ
異なります。`Provider` には最初から3つすべてを定義しています。保存先の識別子はすべての記録に
書き込まれるため、いま書いた記録が後から読めなくなることを避けるためです。この点は最初から
決まっていました。未決定だったのは、最初のリリースで実際に何件が動くかです。

決め手は、Googleドライブが本プロジェクトの作者が日常的に使っている保存先だということです。
アカウントを持っていない保存先は、動作を確認できません。実際のアカウントで一度も実行していない
アップロード処理は、信頼できるものになりません。

**代償も記録しておきます。理論上の話ではなく、実際に発生するためです。** 抽象化の形は
Googleドライブに引きずられます。押し返す相手がいないためです。OneDriveとDropboxを追加する際、
その形は部分的に合わなくなる見込みです。作り直しは #8 の作業に含まれるものであり、今回の判断が
誤っていたことを意味しません。もう一方の選択肢は、3つ同時に作り、3つ目が動くまで何も出さない、
というものでした。

### 削除処理をどこで実行するか（[#3](https://github.com/kaorinstar/driveshot/issues/3)）

**未決定です。**

保存期間の仕組みがあるからこそ、公開状態の共有URLを許容できます。つまり、どれだけ確実に実行
できるかが論点です。

- **アプリ内で完結する方式。** Driveshotが動いているときに、期限切れのファイルを削除します。
  追加で用意するものはなく、サーバーの費用も運用も不要です。ただし、期限を過ぎてもパソコンの
  電源が入っていなければ、次回起動時まで削除されません。
- **サーバーを併用する方式。** 小規模なサービスが期限を管理し、利用者のパソコンの状態に関係なく
  削除します。保存期間が文字どおりの意味になります。その代わり、運用するサービスが1つ増え、
  そのサービスが利用者のクラウドドライブへのトークンを保持することになります。これは、自分の
  トークンだけを持つデスクトップアプリとは、責任の重さが大きく異なります。

後者は前者の小さな拡張ではありません。Driveshotの性質が変わります。利用者のパソコンと利用者の
ドライブだけに触れるアプリから、他者のストレージへの認証情報を預かるサービスになります。採用する
場合は、その機能を含むリリースの前に `SECURITY.ja.md` へ明記する必要があります。

決定するまでの間、coreはどちらでも構築できるように書いてあります。`ShotIndex` は「この時刻の
時点で削除対象となるものはどれか」に答えるだけで、削除自体は行いません。呼び出す側が、定期実行
するアプリなのか別の仕組みなのかは、未定のままにしてあります。

## サインインが依存しているもの、依存していないもの

Driveshotは、外部のスクリーンショット共有サービスが使えなくなったことをきっかけに作られました。
そのため「依存先がなくなったら何が起きるか」は、仮定の話ではありません。プロジェクトを始めた
理由そのものです。答えは、あとから気づくのではなく、あらかじめ書いておきます。

### 2つの約束

これは今の動作の説明ではなく、今後の実装すべてに課す制約です。

1. **キャプチャはサインインの状態に依存しない。** 画面を撮って保存する処理は、ネットワークを
   使わず、どのアカウントも必要としません。ドライブに到達できなくても、サインインが切れていても、
   クライアントが機能しなくなっても、スクリーンショットは撮れます。アップロードが未実装の現在は
   自動的にそうなっていますが、実装後も維持します。
2. **アップロードに失敗したら、画像を端末に残す。** アップロードできなくても、URLを作れなくても、
   手元にはスクリーンショットが残ります。失われるのは手軽さだけで、成果物は失われません。

この2つにより、クラウド側のどの障害も「スクリーンショットを撮って送る」ことを止められません。
共有URLがなかった頃のスクリーンショットツールと同じ動作に戻るだけです。

### サインインが実際に依存しているもの

OAuthクライアントはGoogle Cloudのプロジェクトに属し、そのプロジェクトは1つのアカウントに属します。
Driveshotに同梱するクライアントIDは、そのプロジェクトを指しています。

そのアカウントまたはプロジェクトが停止・削除された場合、次のことが起きます。

- **誰もサインインできなくなります。** 発行済みの更新トークンも使えなくなります。発行元の
  クライアントが存在しなくなるためです。
- **アップロード済みの画像は失われません。** 各利用者自身のDriveにあり、共有URLもそのまま機能
  します。どちらもクライアントIDとは無関係です。
- **Driveshotがそれらを削除できなくなります。** 消えるはずだったファイルが残り、URLを持つ人には
  引き続き見えます。

3つ目が深刻です。このアプリの存在理由そのものが壊れます。次に述べる退避路を、任意ではなく必須と
している理由がこれです。

### 退避路：利用者自身のOAuthクライアント

設定にクライアントIDとシークレットを持たせます。空のままなら同梱のものを使い、入力すればその
利用者自身のものを使います。後者の場合、依存先は利用者自身のプロジェクトだけになります。

これにより、選択肢が3つになります。重要なのは、常に2つ以上あるという点です。

1. **同梱のクライアント。** 設定は不要です。このプロジェクトのGoogle Cloudプロジェクトが存続する
   ことに依存します。
2. **利用者自身のクライアント（Google Workspace）。** 同意画面のユーザーの種類を「内部」にします。
   審査も、ホームページURLも、プライバシーポリシーURLも不要で、「テスト中」に課される更新トークンの
   失効も適用されません。**組織にとってはこれが最も強い選択肢です。** その組織の外側に何も依存せず、
   このプロジェクトのアカウントは経路に一切入りません。
3. **利用者自身のクライアント（個人アカウント）。** 同意画面は「テスト中」のままにします。公開
   ページは不要ですが、7日ごとにサインインし直すことになります。

利用者が自分のクライアントにDriveshotのページを指定することはできません。Googleは、ホームページと
プライバシーポリシーを、開発者自身が所有し所有権を確認できるドメイン上に置くことを求めています。
2番と3番はどちらもページを必要としないため、利用者にWebサイトの用意を求めずに済みます。

### ホスト型サービスとは、リスクの種類が違う

ホスト型のスクリーンショットサービスは、画像を預かります。侵入されれば画像が渡り、停止すれば
画像にアクセスできなくなります。

Driveshotはどちらも預かりません。画像はアップロードの時点から各利用者自身のDriveにあり、トークンは
各利用者自身の端末にあります。**侵入される保管庫が存在しません。** 中間に置く場所そのものがない
ためです。このプロジェクトのGoogle Cloudプロジェクトが失われて損なわれるのは自動化であって、
機密性ではありません。

スコープがさらに範囲を狭めています。Driveshotが要求する `drive.file` は、アプリ自身が作成した
ファイルにしか届きません。トークンが盗まれても、利用者のDriveの他のファイルは読めません。読まない
ように気をつけているのではなく、読む権限を最初から求めていないからです。

これらで解決しない事項も書いておきます。共有URLは、それを持つ人なら誰でも閲覧できます。また、
署名のないインストーラーは、悪意のあるものに差し替えられ得ます（#12）。どちらも `SECURITY.ja.md`
に記載しています。
