# セキュリティ方針

[English version](SECURITY.md)

## 脆弱性の報告方法

GitHubの非公開フォームからご報告ください。

https://github.com/kaorinstar/driveshot/security/advisories/new

通常のIssueは使わないでください。Issueは書いた時点で公開され、取り消せません。

報告には、何が問題か、それによって何ができてしまうか、再現手順の3点があると助かります。
お使いのWindowsまたはmacOSの版と、Driveshotの版も判断の材料になります。

このプロジェクトは1人で開発しているため、すぐには返信できません。7営業日以内に返信します。
期限を過ぎても返信がない場合は、見落としです。お断りしたわけではありませんので、アドバイザリに
追記して再度お知らせください。

## 対象となる版

最新のリリースのみです。修正は、公開済みのリリースに手を入れるのではなく、アプリを新しい版に
置き換える形で届けます。リリースの一覧は次の場所にあります。
https://github.com/kaorinstar/driveshot/releases

## 現在のアプリの動作

報告を判断する基準になるため、ソースを読まなくても分かるようにここに書き出します。
**Driveshotはまだリリースしていません。** 現在の動作は、完成後の動作よりはるかに小さい範囲です。
現状と予定を分けて記載します。予定は、公開後ではなく公開前に設計を検討していただくためのものです。

- 通常のユーザー権限で動作します。管理者権限を求めず、サービスも登録せず、定期実行タスクも作りません。
- 画面の一部を取り込み、画像をユーザーのピクチャフォルダーへ保存します。ネットワークもアカウントも
  使いません。他の機能に何が起きても、この動作は続きます。
- **Googleドライブへのサインインを行います。これが唯一のネットワーク通信です。** 接続先はGoogleの
  2つのホストだけです。ブラウザを `accounts.google.com` へ送り、アプリ自身が
  `oauth2.googleapis.com` へ1回だけHTTPS通信を行い、認可コードをトークンに交換します。
  アップロードは行わず、Driveの内容も読まず、他のどこへも接続しません。
  - サインインはPKCE（RFC 7636）と、RFC 8252のループバックリダイレクトを使います。サインインの
    実行中に限り、`127.0.0.1` 上のポート1つで待ち受けます。ポート番号はOSが選びます。そこへ届いた
    要求は、サインインが送り出した `state` の値を持つ場合にのみ処理します。それ以外は応答して無視し、
    3分で待ち受けを終了します。
  - 要求する権限は `drive.file` だけです。それ未満しか許可されなかった場合は、中途半端に受け入れず
    サインインを失敗として扱います。
- **トークンはメモリ上にのみ保持します。** Driveshotの終了とともに消えるため、起動ごとにサインイン
  が必要です。再起動をまたいで保持するには、保存場所と保護方法を決める必要があります。その決定は、
  実装する変更よりも前に、この文書へ記載します。
- 独自のファイルは1つだけ読みます。存在する場合に限り、設定フォルダー内の `google-client.json` を
  読みます。これは、同梱のものに代えて使う、ユーザー自身のOAuthクライアントを保持するファイルです。
  独自のファイルの書き込みは行いません。設定の永続化は
  [#9](https://github.com/kaorinstar/driveshot/issues/9) です。
- ウィンドウを1つ開き、クラウドドライブ、保存期間の設定、キャプチャのキー、サインインの状態を
  表示します。アップロードは未実装のため、撮影した画像がこの端末を出たことは一度もありません。

## 今後実装する動作

以下はすべて未実装です。セキュリティ上の確認対象として、あらかじめ記載します。

- **画面の一部を取り込む。** macOSでは「画面収録」の許可が必要です。ユーザーがシステム設定で許可します。
  回避方法はなく、Driveshotも回避を試みません。Windowsでは許可は不要です。
- **ユーザー自身のクラウドドライブへアップロードする。** OAuth認証を使います。Googleドライブを
  最初に、単独で実装します（[#2](https://github.com/kaorinstar/driveshot/issues/2)）。OneDriveと
  Dropboxは[#8](https://github.com/kaorinstar/driveshot/issues/8)で対応します。接続したドライブの
  アクセストークンと更新トークンをアプリが保持します。トークンの保存場所と保護方法は未決定です
  （[#6](https://github.com/kaorinstar/driveshot/issues/6)）。実装するリリースの前に、この文書へ
  記載します。
- **Googleへ要求する権限は1つだけにする。** スコープは `drive.file` です。Driveshot自身が作成した
  ファイルにのみ届き、ユーザーのDrive内の他のファイルには届きません。端末からトークンが盗まれても
  Drive全体は読めません。読まないように気をつけているのではなく、読む権限を最初から求めていない
  ためです。これを広げる場合は、実装の詳細ではなく、この文書で報告すべき変更として扱います。
- **共有URLを発行する。** アップロードしたファイルを「リンクを知っている人は閲覧可」に設定します。
  この種のリンクは、URLを知る人なら誰でも画像を見られます。これは機能の目的であると同時に、
  最大のリスクでもあります。次の自動削除は、そのための対策です。
- **保存期間を過ぎたファイルを削除する。** Driveshotが削除するのは、自身がアップロードし、記録を
  持っているファイルだけです。記録にないファイルは削除しません。
- **アップロードの記録を端末内に保持する。** どのドライブに、どのファイルを、いつ保存し、
  どれだけの期間保持するかを記録します。この記録があるため、提供元に問い合わせずに削除を判断できます。

## 1つのGoogle Cloudプロジェクトが担っているもの、担っていないもの

Driveshotは、OAuthのクライアントIDを同梱します。これは1つのアカウントが所有するGoogle Cloudの
プロジェクトに属します。単一障害点であるため、誰かが後から気づくのではなく、ここに明記します。

**そのアカウントまたはプロジェクトが失われた場合**、誰もサインインできなくなり、発行済みの更新
トークンも使えなくなります。アップロード済みの画像は影響を受けません。各利用者自身のDriveにあり、
共有URLも機能します。ただし、**Driveshotが保存期間の満了時にそれらを削除できなくなります。** 消える
はずだったファイルが残り、URLを持つ人には引き続き見えます。これはこのアプリの存在理由そのものの
不具合であり、そのように扱います。

**一方、これは情報漏えいではありません。** 画像やトークンを預かるサーバーが存在しません。画像は
アップロードの時点で利用者自身のDriveへ入り、トークンは利用者自身の端末に残ります。プロジェクトを
失って損なわれるのは自動化であって、機密性ではありません。侵入される保管庫が存在しません。

**範囲を限定する仕組み。** 設定に、利用者自身のクライアントIDとシークレットを持たせます。空のままなら
同梱のものを使います。入力すれば、依存先は利用者自身のプロジェクトだけになります。Google Workspaceでは
そのクライアントを「内部」にでき、審査も公開ページも不要です。個人アカウントでは「テスト中」のままに
でき、こちらも公開ページは不要ですが、7日ごとにサインインし直すことになります。3つの経路は
`docs/architecture.ja.md` に記載しています。

**キャプチャは、これらの後ろにありません。** 画面を撮って保存する処理は、ネットワークもアカウントも
使いません。アップロードに失敗しても画像は端末に残ります。ドライブ、サインイン、クライアントに何が
起きても、スクリーンショットを撮って保存することはできます。

## すでに把握している事項

以下は記載済みの内容です。これらのご報告は、新しい情報にはなりません。

- **コード署名をしていません。** Windowsのインストーラーは署名していないため、初回実行時に
  SmartScreenの警告が出ます。macOS版は署名も公証もしていないため、システム設定で許可するまで
  起動できません。どちらも証明書の取得が解決策ですが、未取得です
  （[#12](https://github.com/kaorinstar/driveshot/issues/12)）。
- **共有URLは、URLを知る人なら誰でも閲覧できます。** 「リンクを知っている人は閲覧可」とは、
  どの提供元でもその意味です。Driveshotの対策は保存期間です。ファイル自体がなくなるため、
  URLも機能しなくなります。
- **保存期間の処理は、Driveshotの動作中のみ実行されます。** 期限切れファイルの削除は、アプリが
  動いているときに行います。パソコンの電源を切ったまま期限を過ぎた場合、そのファイルは次に
  Driveshotを起動するまで残ります。これを許容するか、常時稼働する場所で削除を実行する必要があるかは、
  [#3](https://github.com/kaorinstar/driveshot/issues/3) と
  [docs/architecture.ja.md](docs/architecture.ja.md) に記載した未決定事項です。
